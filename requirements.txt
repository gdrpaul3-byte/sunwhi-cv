# Core — needed to build any artifact.
PyYAML>=6.0

# Optional — only needed for `python scripts/build.py --docx`.
python-docx>=1.1.0

# Optional — only needed for `python scripts/build.py --card` (business card).
# Rendering drives an installed Chrome; no `playwright install` required.
playwright>=1.40
qrcode>=7.4
Pillow>=10.0
opencv-python>=4.8
fonttools>=4.40
PyMuPDF>=1.23
